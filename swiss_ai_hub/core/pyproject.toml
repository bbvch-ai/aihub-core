[project]
name = "swiss-ai-hub-core"
version = "0.270.1"
description = "Swiss AI Hub - Core shared library"
requires-python = ">=3.13, <3.14"
license = "Apache-2.0"
dependencies = []

[build-system]
requires = ["uv_build>=0.10.4,<0.11.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-root = ""
module-name = "swiss_ai_hub.core"
