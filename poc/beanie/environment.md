# Environment

Every check in this spike cites this file. **Fill it before running anything** — a result without a pinned environment
is not reproducible, and FerretDB's MongoDB-compatibility surface changes between releases, so the version *is* the
scope of every finding here.

Do not edit this file retroactively to match a later run. If the environment changes, add a new dated section and note
which checks ran against which.

## Run

| Field                  | Value               |
| ---------------------- | ------------------- |
| Date of run            | *not yet run*       |
| Run by                 | *not yet run*       |
| Git commit (this repo) | *not yet run*       |
| Host OS                | *not yet run*       |

## Database

Expected from `infra/deployment/compose-config.yml`; confirm against the containers actually running, not the config
file — a dev stack drifts.

| Component           | Expected                                   | Actual        |
| ------------------- | ------------------------------------------ | ------------- |
| FerretDB image      | `ferretdb:2.5.0`                           | *not yet run* |
| Backend image       | `postgres-documentdb:17.0.106.0-ferretdb-2.5.0` | *not yet run* |
| Connection string   | from `MongoSettings.CONNECTION_STRING`     | *not yet run* |
| Database name       | dedicated spike DB, never `aihub`          | *not yet run* |
| Reported wire version | `db.runCommand({buildInfo: 1})`          | *not yet run* |

The spike writes to its own database. It must never point at the dev `aihub` database or at `aihub_test`, which the
test suite drops at session start (`packages/core/swiss_ai_hub/core/testing/db_isolation.py`).

## Python packages

| Package     | Version in this repo today | Version used by the spike |
| ----------- | -------------------------- | ------------------------- |
| `beanie`    | not a dependency           | *not yet run*             |
| `pymongo`   | 4.17.0                     | *not yet run*             |
| `motor`     | 3.7.1 (transitive)         | *not yet run*             |
| `mongoengine` | 0.29.3                   | *not yet run*             |
| Python      | *not yet run*              | *not yet run*             |

Beanie is added on this branch only, and never to a package's `pyproject.toml` that could reach `main`.

Note on the driver: Beanie 2.x dropped Motor in favour of PyMongo's own async client (`AsyncMongoClient`). Motor is
deprecated upstream, so a Beanie version still built on it would be a finding in its own right — record which driver the
installed version actually uses.

## Stack state

| Question                                | Answer        |
| --------------------------------------- | ------------- |
| Freshly reset stack, or long-running?   | *not yet run* |
| Other services running against the same FerretDB? | *not yet run* |
| Any non-default FerretDB configuration? | *not yet run* |

A long-running dev stack with other services writing to the same FerretDB is acceptable for compatibility checks, but
not for the phase-3 load measurement — note it here so a later reader knows which figures are contaminated.
