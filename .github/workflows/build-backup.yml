---
name: Build and Release Backup Service Image
on:
  repository_dispatch:
    types: [release-ready]
  workflow_dispatch:
    inputs:
      release_version:
        description: Docker image release version
        required: true
        default: manual
      secondary_tag:
        description: Secondary tag to apply (optional), [nightly, latest, *empty*]
        required: false
        default: ''
        options: ['', nightly, latest]
permissions:
  contents: write
  packages: write
jobs:
  build:
    runs-on: ubuntu-latest
    if: >
      github.event_name == 'workflow_dispatch' ||
      github.event_name == 'repository_dispatch'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Extract Tag / Version
        id: get_tag
        env:
          INPUT_VERSION: ${{ github.event.inputs.release_version }}
          PAYLOAD_VERSION: ${{ github.event.client_payload.release_version }}
        run: |
          # 1) Prefer workflow_dispatch input
          if [ -n "$INPUT_VERSION" ] && [ "$INPUT_VERSION" != "manual" ]; then
            TAG_NAME="$INPUT_VERSION"
            echo "Using release version from workflow_dispatch input: $TAG_NAME"

          # 2) Then repository_dispatch client_payload
          elif [ -n "$PAYLOAD_VERSION" ]; then
            TAG_NAME="$PAYLOAD_VERSION"
            echo "Using release version from repository_dispatch payload: $TAG_NAME"

          # 3) No Fallback
          else
            echo "No release_version provided. Failing."
            exit 1
          fi
          echo "tag=$TAG_NAME" >> $GITHUB_OUTPUT
      - name: build and release docker image
        uses: ./aihub_action/build_image
        with:
          release_version: ${{ inputs.release_version || steps.get_tag.outputs.tag || format('testing-{0}', github.run_number) }}
          image_name: backup
          github_repository_owner: ${{ github.repository_owner }}
          github_event_repository_name: ${{ github.event.repository.name }}
          context: .
          file: ./aihub_backup/Dockerfile
          github_token: ${{ secrets.GITHUB_TOKEN }}
          secondary_tag: ${{ github.event_name == 'repository_dispatch' && 'nightly' || github.event.inputs.secondary_tag }}
