{#- This file is auto-generated from a Jinja2 template. -#}
{#- DO NOT EDIT THIS FILE DIRECTLY. -#}
{#- -#}
{#- To make changes, edit deployment/templates/configs/keycloak/bootstrap/users-superuser.json.j2 -#}
{#-
   BOOTSTRAP-ONLY: applied via --import-realm on first start, never reconciled.
   The superuser is a seed — password changes and other runtime edits must
   survive restarts (ADR 2026_04_14_superuser_via_keycloak_realm_role).
   The quoted "$(envjson:...)" sentinel keeps this file valid JSON for the
   generator merge; keycloak-entrypoint.sh replaces the token including the
   surrounding quotes with the raw JSON array from the environment.
-#}
{
  "realm": "aihub",
  "users": [
    {
      "username": "$(env:SUPERUSER_EMAIL)",
      "email": "$(env:SUPERUSER_EMAIL)",
      "firstName": "$(env:SUPERUSER_FIRSTNAME)",
      "lastName": "$(env:SUPERUSER_LASTNAME)",
      "enabled": true,
      "emailVerified": true,
      "credentials": [
        {
          "type": "password",
          "value": "$(env:SUPERUSER_PASSWORD)",
          "temporary": false
        }
      ],
      "realmRoles": "$(envjson:SUPERUSER_ROLES_JSON)"
    }
  ]
}
