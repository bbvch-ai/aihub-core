{#- This file is auto-generated from a Jinja2 template. -#}
{#- DO NOT EDIT THIS FILE DIRECTLY. -#}
{#- -#}
{#- To make changes, edit deployment/templates/configs/keycloak/managed/10-roles.json.j2 -#}
{#-
   MANAGED: reconciled on every start by keycloak-config-cli (file wins,
   full reconcile — realm roles removed here are deleted from running realms;
   Keycloak built-in roles are never touched).
   All realm roles must live in this single file: keycloak-config-cli processes
   each file as a separate full-managed import, so a second file containing
   "roles" would delete the roles defined here.
   The numeric filename prefix fixes the lexicographic import order: roles
   must exist before the auth flows (40) that reference AIHubAccess.
-#}
{
  "realm": "aihub",
  "roles": {
    "realm": [
      {
        "name": "AIHubAccess",
        "description": "Required role to access AI-Hub. Users without this role are denied at login."
      },
      {
        "name": "AIHubSysAdmin",
        "description": "System administrator access to infrastructure tools (Dagster, SeaweedFS, Attu, etc.)"
      }
    ]
  }
}
