{#- This file is auto-generated from a Jinja2 template. -#}
{#- DO NOT EDIT THIS FILE DIRECTLY. -#}
{#- -#}
{#- To make changes, edit deployment/templates/configs/keycloak/managed/60-service-accounts.json.j2 -#}
{#-
   MANAGED: reconciled on every start by keycloak-config-cli, so changes to
   the realm-management roles below reach running deployments. Users are never
   a full-managed type in keycloak-config-cli — only the users listed here are
   updated; all other users (superuser, real users) are never touched or
   deleted. Imported after the clients (30) so the service-account client
   exists.

   The current minimum role set required for the API to function correctly:
   view-realm and view-clients are required by the realm-role-members endpoint
   (GET /admin/realms/{realm}/roles/{role}/users) used to resolve sysadmin
   status.
-#}
{
  "realm": "aihub",
  "users": [
    {
      "username": "service-account-aihub-api-service",
      "enabled": true,
      "serviceAccountClientId": "aihub-api-service",
      "clientRoles": {
        "realm-management": ["view-identity-providers", "manage-users", "view-users", "query-users", "query-groups", "view-groups", "view-realm", "view-clients"]
      }
    }
  ]
}
