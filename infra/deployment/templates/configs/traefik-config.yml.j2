# This file is auto-generated from a Jinja2 template.
# DO NOT EDIT THIS FILE DIRECTLY.
#
# To make changes, edit the following files and then run the generator script:
#   - deployment/compose-config.yml (for changing values and versions)
#   - deployment/templates/traefik.yml.j2 (for changing structure and logic)

# ===================================================================
# Traefik Static Configuration
# ===================================================================
# This file contains the static configuration for Traefik reverse proxy.
# It defines entry points, providers, API settings, and certificate resolvers.
# ===================================================================

entryPoints:
  web:
    address: ":80"
  websecure:
    address: ":443"
{%- if stage in ['dev', 'local', 'build'] %}
    http:
      tls: true
{%- endif %}

providers:
  file:
    directory: /etc/traefik/dynamic
  docker:
    endpoint: "tcp://docker-socket-proxy:2375"
    watch: true
    exposedByDefault: false

api:
  dashboard: true
{%- if stage in ['dev', 'local', 'build'] %}
  insecure: true
{%- endif %}

{%- if stage not in ['dev', 'local', 'build'] %}
certificatesResolvers:
  letsencrypt:
    acme:
      storage: /etc/traefik/acme.json
      httpChallenge:
        entryPoint: web
{%- endif %}

