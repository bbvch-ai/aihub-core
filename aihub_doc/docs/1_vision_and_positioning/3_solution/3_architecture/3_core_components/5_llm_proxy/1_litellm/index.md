AI Hub istelf can work with any LLM that implements the state of the art interfaces. through litellm we can furthermore
abstract and manage the access to those modes and define our own internal endpoints which allows for all switching and
control on this proxy level without the need of code adjustments in downstream applications.
