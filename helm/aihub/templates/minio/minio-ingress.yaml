{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "aihub.fullname" (dict "component" "minio-ingress" "context" .) }}
  labels:
    {{- include "aihub.labels" . | nindent 4 }}
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "0"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "600"
spec:
  ingressClassName: {{ .Values.ingress.className }}
  tls:
    - secretName: {{ include "aihub.fullname" (dict "component" "minio-tls" "context" .) }}
      hosts:
        - "datalake.{{ .Values.global.domain }}"
  rules:
    - host: "datalake.{{ .Values.global.domain }}"
      http:
        paths:
          # MinIO service (priority 1000 - Service-specific catch-all routes)
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "aihub.fullname" (dict "component" "minio" "context" .) }}
                port:
                  number: 9001
{{- end }}
