{{- if .Values.ragAgent.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "aihub.fullname" (dict "component" "rag-agent" "context" .) }}
  labels:
    {{- include "aihub.labels" . | nindent 4 }}
    app.kubernetes.io/component: rag-agent
spec:
  replicas: {{ .Values.ragAgent.replicas }}
  selector:
    matchLabels:
      {{- include "aihub.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: rag-agent
  template:
    metadata:
      labels:
        {{- include "aihub.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: rag-agent
    spec:
      securityContext:
        {{- include "aihub.podSecurityContext" . | nindent 8 }}
      tolerations:
        {{- include "aihub.globalTolerations" . | nindent 8 }}
      containers:
      - name: rag-agent
        image: {{ .Values.ragAgent.image.repository }}:{{ .Values.ragAgent.image.tag }}
        imagePullPolicy: {{ .Values.ragAgent.image.pullPolicy }}
        securityContext:
          {{- include "aihub.securityContext" . | nindent 10 }}
        env:
        {{- include "aihub.commonEnv" . | nindent 8 }}
        - name: NATS_ENDPOINT
          value: "nats://{{ include "aihub.fullname" (dict "component" "nats" "context" .) }}:4222"
        - name: REDIS_URL
          value: "redis://{{ include "aihub.fullname" (dict "component" "redis" "context" .) }}:6379"
        - name: PHOENIX_ENDPOINT
          value: "http://{{ include "aihub.fullname" (dict "component" "phoenix" "context" .) }}:6006"
        - name: LITE_LLM_PROXY_BASE_URL
          value: "http://{{ include "aihub.fullname" (dict "component" "litellm" "context" .) }}:4000"
        - name: LITE_LLM_PROXY_API_KEY
          valueFrom:
            configMapKeyRef:
              name: {{ include "aihub.fullname" (dict "component" "env" "context" .) }}
              key: LITELLM_MASTER_KEY
        - name: MILVUS_URL
          value: "http://{{ include "aihub.fullname" (dict "component" "milvus" "context" .) }}"
        - name: MILVUS_DIMENSION
          value: "3072"
        resources:
          {{- toYaml .Values.ragAgent.resources | nindent 10 }}
      volumes:
{{- end }}
