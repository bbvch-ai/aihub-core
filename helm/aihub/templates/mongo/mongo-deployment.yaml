{{- if .Values.mongo.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "aihub.fullname" (dict "component" "mongo" "context" .) }}
  labels:
    {{- include "aihub.labels" . | nindent 4 }}
    app.kubernetes.io/component: mongo
spec:
  replicas: {{ .Values.mongo.replicas }}
  selector:
    matchLabels:
      {{- include "aihub.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: mongo
  template:
    metadata:
      labels:
        {{- include "aihub.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: mongo
    spec:
      {{- include "aihub.imagePullSecrets" . | nindent 6 }}
      securityContext:
        {{- include "aihub.podSecurityContext" . | nindent 8 }}
      tolerations:
        {{- include "aihub.globalTolerations" . | nindent 8 }}
      containers:
      - name: mongo
        image: {{ .Values.mongo.image.repository }}:{{ .Values.mongo.image.tag }}
        imagePullPolicy: {{ .Values.mongo.image.pullPolicy }}
        securityContext:
          {{- include "aihub.securityContext" . | nindent 10 }}
        command:
        - mongod
        - --quiet
        env:
        {{- include "aihub.commonEnv" . | nindent 8 }}
        envFrom:
        # Sensitive environment variables from Mongo Secret
        - secretRef:
            name: {{ include "aihub.fullname" (dict "component" "mongo-secret" "context" .) }}
        ports:
        - containerPort: 27017
          name: mongo
        volumeMounts:
        - name: mongo-data
          mountPath: /data/db
        livenessProbe:
          exec:
            command:
            - mongosh
            - --eval
            - "db.runCommand('ping').ok"
            - localhost:27017/test
            - --quiet
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          exec:
            command:
            - mongosh
            - --eval
            - "db.runCommand('ping').ok"
            - localhost:27017/test
            - --quiet
          initialDelaySeconds: 5
          periodSeconds: 5
        resources:
          {{- toYaml .Values.mongo.resources | nindent 10 }}
      volumes:
      - name: mongo-data
        persistentVolumeClaim:
          claimName: {{ include "aihub.fullname" (dict "component" "mongo-data" "context" .) }}
{{- end }}
